services:
  db:
    restart: unless-stopped

  backend:
    restart: unless-stopped
    ports:
      - "127.0.0.1:8000:8000"

  frontend:
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:80"
